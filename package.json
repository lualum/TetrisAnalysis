{
   "name": "tetris-typescript",
   "version": "1.0.0",
   "description": "Tetris game in TypeScript with class-based architecture",
   "type": "module",
   "scripts": {
      "dev": "vite",
      "build": "tsc && vite build",
      "build:blockfish-wasm": "cd ../blockfish-dev && wasm-pack build blockfish-wasm --target web --out-dir ../../TetrisAnalysis/src/blockfish-wasm-pkg && rm -f ../TetrisAnalysis/src/blockfish-wasm-pkg/.gitignore",
      "preview": "vite preview",
      "type-check": "tsc --noEmit",
      "blockfish:smoke": "node scripts/blockfish-smoke.mjs"
   },
   "devDependencies": {
      "@types/node": "^20.19.43",
      "typescript": "^5.3.3",
      "vite": "^5.4.21"
   },
   "dependencies": {
      "pixi.js": "^8.19.0"
   }
}
